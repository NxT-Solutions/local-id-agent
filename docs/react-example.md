# React example

See [examples/react/README.md](../examples/react/README.md) for the full guide.

Quick start:

```bash
cd services/agent && go run ./cmd/localid-agent --config config.example.json
cd services/agent && go run ./cmd/mock-backend   # or FastAPI / Laravel
pnpm run dev:react
```

Other frontends: [Vue](../vue/README.md). Other backends: [FastAPI](../fastapi/README.md), [Laravel](../laravel/README.md).
